.DS_Store
src/.DS_Store
build/

# Dependencies
node_modules/
vendor/

# Local wp-env personal overrides
.wp-env.override.json

# plugin-zip output, and where CI unpacks it to run plugin-check
ceros.zip
dist/

# PHPUnit's test-order cache, and a local phpunit.xml override
.phpunit.result.cache
phpunit.xml
